# 004: Interactive TUI for issue creation

This packet specifies `afctl issue create-form`, a terminal-based interactive UI for creating issues with dependencies and context in a guided wizard, using `charmbracelet/huh`.
