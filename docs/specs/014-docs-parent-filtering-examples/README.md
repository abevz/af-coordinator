# 014 Docs Parent Filtering Examples

Status: complete. See `requirements.md`, `design.md`, `tasks.md`, `review.md`.

Add clear CLI examples to `docs/workflows-v1.md` and `docs/operations.md` for querying and filtering child tasks by their `parent` dependency relationship (e.g., using `grep` with human-readable table output and `jq` with `--json` output).

- `docs/workflows-v1.md`: Add a section on querying/filtering child tasks of a parent epic.
- `docs/operations.md`: Expand the `DEPS` column documentation with explicit filtering patterns.
